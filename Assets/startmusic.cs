using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class startmusic : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //AkSoundEngine.PostEvent("TitleMusic", gameObject);
    }
	
	// Update is called once per frame
	void Update () {
    }
}
